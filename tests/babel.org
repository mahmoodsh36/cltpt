#+begin_src python
  text = "block1"
  print(text)
#+end_src

#+begin_src python :name blk0
  text = "block2"
  print(text)
  print(text)
#+end_src

#+begin_src python :exports both :results %'(:id link-dest :pattern (cltpt/combinator:atleast-one-discard (cltpt/combinator:all-but-newline))) :transform %(lambda (out-rdr match) (format nil "[[~A]]" (cltpt/combinator:match-text match out-rdr))) :name blk1
  filepath = "out.png"
  print(filepath)
#+end_src

#+begin_src python :name image-handler
  return { "path": "link-path", "type": "link-type" }
#+end_src

#+begin_src python :results value :name blk8
  return [1, 2, 3]
#+end_src

#+begin_src python :name word-handler :var a1=blk8
  print('word ' + str(a1))
#+end_src

#+begin_src python :name blk0-child-handler
  child = [%(getf child :match), %(getf child :begin), %(getf child :end)]
  return child
#+end_src

#+begin_src python :name blk0-tree-handler
  class blk0_tree:
      def __init__(self):
          self.prop = 1

  tree = blk0_tree()
  %'blk0-child-handler-loop
#+end_src

#+begin_src python :exports both :input blk1 :input-type blk1 object :input-handler blk1 org-link image-handler :input blk0 :input-type blk0 stream :input-handler blk0 word-handler :input-rule %'(:name blk0-tree :pattern (cltpt/combinator:separated-atleast-one ,(string #\newline) (cltpt/combinator:all-but-newline)))
  print(blk1)
  for word in %'blk0-tree-handler:
      print(word)
#+end_src

#+begin_src c
  printf("hello from C\n");
#+end_src

#+begin_src python :results value :name pynums
  return [10, 20, 30]
#+end_src

#+begin_src c :var xs=pynums
  int total = 0;
  for (unsigned int i = 0; i < xs_len; i++) total += xs[i];
  printf("sum of %lu items = %ld\n", xs_len, total);
#+end_src

#+begin_src c :main no
  #include <stdio.h>
  int main(void) {
      printf("unwrapped %d\n", 99);
      return 0;
  }
#+end_src